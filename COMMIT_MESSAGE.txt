fix: corrige erro de build do TypeScript no useImport

- Adiciona campo opcional `reason` na interface FailedImport,
  resolvendo erro de build "Object literal may only specify known
  properties" no guard anti-duplicata da importação
- Garante que o campo `error` (obrigatório) também é preenchido
  no push de duplicata ignorada
- Move favicon.svg para public/ para que o Vite o inclua
  corretamente no build de produção